<h2>New location</h2>

<%= error_messages_for :location %>

<% form_for(:location, :url => locations_path) do |f| %>
  
	<%= render :partial => "form" %>
	<%= submit_tag _("Create") %>
<% end %>

<%= link_to _('Back'), locations_path %>